/* eslint-disable import/no-anonymous-default-export */
import { checkerData } from "./data";
import {
  checkerColumns,
  voucherCategoryColumns,
  voucherTypeColumns,
  shopRows,
} from "./columns";

export default {
  //Data
  checkerData,
  //Columns
  checkerColumns,
  voucherCategoryColumns,
  voucherTypeColumns,
  shopRows,
};
